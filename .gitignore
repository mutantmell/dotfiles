.direnv/
secrets/*
.gcroots
result*
*~
# LUKS encryption keyfiles
.keys/
*.key
# Ignore temp data
temp/
# OpenWrt build artifacts (may contain secrets)
openwrt-images/
# Claude Code per-user local settings (machine-specific, not shared)
.claude/settings.local.json
