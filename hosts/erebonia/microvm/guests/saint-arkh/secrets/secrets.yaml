# Placeholder — re-encrypt with saint-arkh's SSH key after deployment
# sops --encrypt --age $(ssh-to-age < /path/to/saint-arkh/ssh_host_ed25519_key.pub) secrets.yaml
forgejo-runner-token: PLACEHOLDER
sops:
  lastmodified: "2025-01-01T00:00:00Z"
  version: 3.7.3
